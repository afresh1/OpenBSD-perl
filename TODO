Once we have something working, for vax, we need to have pre-built unicore
* script to regenerate MANIFEST
* script to update the RCS Id's on files/*
* script to regenerate MANIFEST (again)
* Check that perl doesn't rebuild when nothing has changed.
* check DynaLoader.sed
* More fixes for vax
* Default NGROUPS_MAX https://www.j3e.de/ngroups.html
